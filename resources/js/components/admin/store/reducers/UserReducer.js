const initialState = {
    isAuthenticated: false
}

const userReducer = (state = initialState, action) => {
    switch (action.data) {
        default:
            return state

    }
}

export default userReducer